
    /*
        As interfaces são padrões definidos através de contratos ou específicações 
        Um contrato define um determinado conjunto de métodos que serão implemen-
        tados nas classes que assinarem esse contrato.
        Vale dizer também que a criação das interfaces só foi necessaria porque,
        o java não aceita a chamação de duas abstratas 
    */
public interface Interfaces  {
    void emitirSom();
    void pedindoComida();
}
