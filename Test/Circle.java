public class Circle {
    double rx, ry;
    double vx, vy;
//criando a funcao de criar os circulos passando como parametros as cordenadas de X e Y e a velocidade em ambos os eixos
    Circle(double rx, double ry, double vx, double vy) {
        this.rx = rx;
        this.ry = ry;
        this.vx = vx;
        this.vy = vy;

    }
}
